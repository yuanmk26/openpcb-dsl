# AGENTS

## 文档语言要求

- 仓库中的文档描述默认使用中文。
- 包括但不限于 `README.md`、`docs/` 下文档、示例说明、注释性说明文字、面向协作者的约定。
- 只有在必须保留英文原文的场景下，才允许保留英文；此时应优先补充中文说明。

## 协作要求

- 修改或新增文档时，优先保持现有中文表述风格，避免中英混写。
- 新增对外说明时，默认先写中文，再根据需要补充英文术语或代码标识。
- API 名称、代码标识、文件路径、命令行片段保持原样，不做中文化。
- 每次完成修改后，执行 `git add` 和 `git commit`，确保变更及时纳入版本管理。
